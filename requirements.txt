# Runtime
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
pydantic>=2.7

# Parsing Showdown's formats-data.js. The fallback parser in format_service
# handles the real file without this, but json5 covers all of JS object syntax
# and is what the parser prefers when it is installed.
json5>=0.9

# Tests
pytest>=8.0
