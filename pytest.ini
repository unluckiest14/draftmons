[pytest]
# Only tests/ — scripts/smoke_test.py matches pytest's default *_test.py
# pattern, and it sets DRAFT_DB and imports the app at module level. Collected
# alongside the real suite it rewrote the environment other tests had already
# bound their modules against, which surfaced as unrelated tests failing on a
# database that was not theirs.
testpaths = tests
